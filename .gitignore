# ── OS ────────────────────────────────────────────────────────────────────

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
desktop.ini
$RECYCLE.BIN/

# Linux
*~
.directory

# ── Editor / IDE ──────────────────────────────────────────────────────────

# JetBrains (IntelliJ, PyCharm, GoLand, Rider, etc.)
.idea/

# VS Code
.vscode/

# Vim / Neovim
*.swp
*.swo
Session.vim

# Emacs
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc

# ── Secrets & credentials ─────────────────────────────────────────────────
# IMPORTANT: Never commit secrets. Use environment variables or a secrets manager.

.env
.env.*
!.env.example
!.env.sample
!.env.template

# Private keys & certificates
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.ppk

# Secret / config files that may contain credentials
secrets.json
secrets.yaml
secrets.yml
*.secret
.secrets/
local.settings.json

# ── Logs ──────────────────────────────────────────────────────────────────

*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pip-log.txt

# ── Docker ────────────────────────────────────────────────────────────────

# Local overrides — keep base compose files, ignore local overrides
docker-compose.override.yml
docker-compose.override.yaml
docker-compose.local.yml
docker-compose.local.yaml

# ── Coverage / test reports ───────────────────────────────────────────────

coverage/
.coverage
htmlcov/
*.lcov
test-results/
junit.xml

# ── Tool caches (cross-project) ───────────────────────────────────────────

.cache/
.snyk

# ── Temporary files ───────────────────────────────────────────────────────

*.tmp
*.temp
*.bak
*.orig
